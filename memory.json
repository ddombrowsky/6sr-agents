{
  "agent_input_files": "slot-1.txt, slot-2.txt, and slot-3.txt in /opt/agents/swarm/ are agent input files: swarm_monitor.py pipes whichever slot file changes as the opening turn into a fresh emperor-agent.py process (each run is a brand-new interpreter with no memory of prior runs, so this fact only persists via the remember tool / memory.json).",
  "emperor_review_20260821": "Emperor review pass on 2026-08-21: Added CYCLE_SLEEP throughput warning to monitor.py (startup note when >2h, currently 28800s=8h=13% of default throughput). Added live_track_record gate to domain_sdex_maker.py (checks replay net_edge_usd > 0 and inventory inside band before real-money promotion; fails closed). Updated domain.py contract docs. Commit 60c20c5 in /opt/master_agent. Key findings: all top-8 makers had negative 7d replay net edge but positive live scores from XLM rally inflating inventory; 5 strategies past $250 inventory band; CYCLE_SLEEP=28800 is 8x slower than designed."
}